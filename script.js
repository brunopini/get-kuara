function storeValues() {
    const name = document.getElementById('name').value;
    const advertiserId = document.getElementById('advertiser_id').value;

    console.log("Stored values:", name, advertiserId);
    // Here, we're just logging the values to the console.
    // You can later modify this to do something else with the values.
}
